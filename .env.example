# Placeholders only. Generate each secret independently with:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
DATABASE_URL=postgres://planner:replace-password@localhost:5432/planner
PUBLIC_ORIGIN=http://localhost:4321
INVITE_TOKEN=replace-with-a-random-secret-at-least-32-characters
SESSION_SECRET=replace-with-another-random-secret-at-least-32-characters
HOST=0.0.0.0
PORT=3000
# Destructive test setup is restricted to database names ending in _e2e.
E2E_DATABASE_URL=postgres://planner:replace-password@localhost:5432/planner_e2e
